{
  "name": "shipit-board",
  "version": "1.0.0",
  "private": true,
  "description": "A small full-stack task board for DevOps practice",
  "main": "server.js",
  "scripts": {
    "start": "node frontend-server.js",
    "start:api": "node server.js",
    "start:frontend": "node frontend-server.js",
    "dev": "nodemon frontend-server.js",
    "dev:api": "nodemon server.js",
    "test": "node --test",
    "lint": "node --check server.js && node --check frontend-server.js && node --check public/app.js && node --check test/app.test.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
