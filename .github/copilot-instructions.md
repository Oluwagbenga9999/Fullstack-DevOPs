# Workspace Instructions

- Keep the ShipIt Board project intentionally small and dependency-light.
- Use CommonJS JavaScript and the built-in Node test runner.
- Preserve the separation between the Express API and the static frontend.
- Validate changes with `npm run lint` and `npm test`.
